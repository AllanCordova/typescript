import Angulo from "./angulo";

const horaX: Angulo = new Angulo(11, 30);

console.log(`${horaX.toString()}`);

const horaY: Angulo = new Angulo(12, 0);

console.log(`${horaY.toString()}`);

const horaA: Angulo = new Angulo(24, 59);

console.log(`${horaA.toString()}`);